apiVersion: v1
kind: Service
metadata:
    name: jenkins-service
    namespace: jenkins-dev
spec:
    selector:
        app: jenkins
    ports:
        -   protocol: TCP
            port: 7070
            targetPort: 7070
            nodePort: 30000
    type: NodePort
